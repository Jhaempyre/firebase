{
  "name": "server",
  "version": "1.0.0",
  "description": "run a server will listen to some files ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -r dotenv/config --experimental-json-modules src/server.js"
  },
  "author": "jhaempire",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prettier": "^3.2.2"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase": "^10.8.0"
  }
}
